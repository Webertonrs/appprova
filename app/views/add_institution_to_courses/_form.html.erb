<%= form_with(model: add_institution_to_course, local: true) do |form| %>
  <% if add_institution_to_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_institution_to_course.errors.count, "error") %> prohibited this add_institution_to_course from being saved:</h2>

      <ul>
      <% add_institution_to_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :institution_id %>
    <%= form.text_field :institution_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
